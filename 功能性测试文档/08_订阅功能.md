# 08. 订阅功能

> **模块说明**：本模块覆盖标签点属性更改订阅、快照值更改订阅（含增量订阅）、订阅修改与取消，以及数据流（Datagram）的创建、接收与删除等相关 Raw API。

---

## 一、标签点属性订阅

### 1. RawRtdbbSubscribeTagsExWarp — 标签点属性更改通知订阅

**函数签名**：`func RawRtdbbSubscribeTagsExWarp(handle ConnectHandle, options RtdbSubscribeOption, name string) (unsafe.Pointer, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-SUBTAG-01 | 正常创建属性订阅 | 已登录，handle 独立未用于其他 API | options=RTDB_O_AUTOCONN, name="tagSub1" | 调用 `RawRtdbbSubscribeTagsExWarp` | 返回 `RteOk`，unsafe.Pointer 非 nil | P1 |
| TC-SUBTAG-02 | 无自动重连订阅 | 已登录 | options=0 | 调用 `RawRtdbbSubscribeTagsExWarp` | 返回 `RteOk`，网络断开后回调 RTDB_E_DISCONNECT 并退出 | P2 |
| TC-SUBTAG-03 | 使用已用于其他 API 的句柄 | 已登录，该句柄已调用过查询 API | — | 调用 `RawRtdbbSubscribeTagsExWarp` | 返回句柄已占用错误或警告 | P3 |
| TC-SUBTAG-04 | 未登录 | 仅连接未登录 | — | 调用 `RawRtdbbSubscribeTagsExWarp` | 返回 `RteHaveNotLogin` | P2 |

---

### 2. RawRtdbbCancelSubscribeTagsWarp — 取消标签点属性更改通知订阅

**函数签名**：`func RawRtdbbCancelSubscribeTagsWarp(handle ConnectHandle, param unsafe.Pointer) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-CANTAG-01 | 正常取消订阅 | 已创建属性订阅 | param=SubscribeTagsEx 返回的指针 | 调用 `RawRtdbbCancelSubscribeTagsWarp` | 返回 `RteOk`，不再收到回调 | P1 |
| TC-CANTAG-02 | 重复取消 | 已取消 | 同上 | 再次调用 `RawRtdbbCancelSubscribeTagsWarp` | 返回 `RteOk` 或错误 | P3 |
| TC-CANTAG-03 | 无效句柄 | handle = -1 | param 有效 | 调用 `RawRtdbbCancelSubscribeTagsWarp` | 返回 `RteInvalidHandle` | P2 |

---

## 二、快照值订阅

### 3. RawRtdbsSubscribeSnapshotsEx64Warp — 批量标签点快照改变通知订阅

**函数签名**：`func RawRtdbsSubscribeSnapshotsEx64Warp(handle ConnectHandle, ids []PointID, options RtdbSubscribeOption, name string) (unsafe.Pointer, []RtdbError, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-SUBSNAP-01 | 正常订阅快照变更 | 已登录，handle 独立 | ids=[1,2,3], options=RTDB_O_AUTOCONN | 调用 `RawRtdbsSubscribeSnapshotsEx64Warp` | 返回 `RteOk`，errs 全 0，pointer 非 nil | P1 |
| TC-SUBSNAP-02 | 含无效 ID | 已登录 | ids=[1,999999] | 调用 `RawRtdbsSubscribeSnapshotsEx64Warp` | 返回 `RteOk`，errs[1] 非 0，有效 ID 订阅成功 | P2 |
| TC-SUBSNAP-03 | 空 ID 数组 | 已登录 | ids=[] | 调用 `RawRtdbsSubscribeSnapshotsEx64Warp` | 返回参数错误或空订阅 | P3 |
| TC-SUBSNAP-04 | 网络断开后自动恢复 | 已登录，options=RTDB_O_AUTOCONN | — | 模拟网络中断后恢复 | 回调 RTDB_E_DISCONNECT，随后 RTDB_E_RECOVERY | P2 |
| TC-SUBSNAP-05 | 快照变化触发回调 | 已订阅 | 写入被订阅点新值 | 写入操作 | 回调收到 RTDB_E_DATA，含该点 ID 和新值 | P1 |

---

### 4. RawRtdbsSubscribeDeltaSnapshots64Warp — 增量订阅快照变更

**函数签名**：`func RawRtdbsSubscribeDeltaSnapshots64Warp(handle ConnectHandle, ids []PointID, deltaValues []float64, deltaStates []int64, options RtdbSubscribeOption, name string) (unsafe.Pointer, []RtdbError, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-SUBDELTA-01 | 正常增量订阅 | 已登录 | ids=[1], deltaValues=[1.0], deltaStates=[0] | 调用 `RawRtdbsSubscribeDeltaSnapshots64Warp` | 返回 `RteOk` | P1 |
| TC-SUBDELTA-02 | 变化未超容差不触发 | 已订阅，deltaValue=10.0 | 写入变化 5.0 | 写入 | 不触发回调 | P1 |
| TC-SUBDELTA-03 | 变化超容差触发 | 已订阅，deltaValue=10.0 | 写入变化 15.0 | 写入 | 触发回调 RTDB_E_DATA | P1 |
| TC-SUBDELTA-04 | deltaValues/deltaStates 为空 | 已登录 | deltaValues=[], deltaStates=[] | 调用 | 返回参数错误或视为 0 容差 | P3 |

---

### 5. RawRtdbsChangeSubscribeSnapshotsWarp — 批量修改订阅标签点信息

**函数签名**：`func RawRtdbsChangeSubscribeSnapshotsWarp(handle ConnectHandle, ids []PointID, deltaValues []float64, deltaStates []int64, changedTypes []RtdbSubscribeChangeType) ([]RtdbError, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-CHGSUB-01 | 新增订阅标签点 | 已存在快照订阅 | ids=[新增点ID], changedTypes=[ChangeAdd] | 调用 `RawRtdbsChangeSubscribeSnapshotsWarp` | 返回 `RteOk`，回调随后包含新增点 | P1 |
| TC-CHGSUB-02 | 删除订阅标签点 | 已存在快照订阅 | ids=[已订阅点ID], changedTypes=[ChangeRemove] | 调用 `RawRtdbsChangeSubscribeSnapshotsWarp` | 返回 `RteOk`，该点变更不再触发回调 | P1 |
| TC-CHGSUB-03 | 修改容差值 | 已存在增量订阅 | ids=[已订阅点], deltaValues=[新容差] | 调用 `RawRtdbsChangeSubscribeSnapshotsWarp` | 返回 `RteOk`，按新容差触发 | P2 |
| TC-CHGSUB-04 | 未订阅直接修改 | 无活跃订阅 | — | 调用 `RawRtdbsChangeSubscribeSnapshotsWarp` | 返回无活跃订阅错误 | P2 |

---

### 6. RawRtdbsCancelSubscribeSnapshotsWarp — 取消标签点快照更改通知订阅

**函数签名**：`func RawRtdbsCancelSubscribeSnapshotsWarp(handle ConnectHandle, param unsafe.Pointer) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-CANSNAP-01 | 正常取消快照订阅 | 已创建快照订阅 | param=SubscribeSnapshotsEx64 返回的指针 | 调用 `RawRtdbsCancelSubscribeSnapshotsWarp` | 返回 `RteOk`，不再收到回调 | P1 |
| TC-CANSNAP-02 | 取消后再次写入 | 已取消 | — | 写入原订阅点 | 无回调触发 | P2 |
| TC-CANSNAP-03 | 重复取消 | 已取消 | param 同上 | 再次调用 | 返回 `RteOk` 或错误 | P3 |

---

## 三、数据流（Datagram）

### 7. RawRtdbCreateDatagramHandleWarp — 创建数据流

**函数签名**：`func RawRtdbCreateDatagramHandleWarp(port int32, remoteHost string) (DatagramHandle, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-DGRAMCRT-01 | 正常创建数据流 | 无 | port=9001, remoteHost="127.0.0.1" | 调用 `RawRtdbCreateDatagramHandleWarp` | 返回 `RteOk`，DatagramHandle 有效 | P1 |
| TC-DGRAMCRT-02 | 非法端口 | 无 | port=-1 | 调用 | 返回 `RteInvalidParameter` | P2 |
| TC-DGRAMCRT-03 | 端口超出范围 | 无 | port=70000 | 调用 | 返回 `RteInvalidParameter` | P2 |
| TC-DGRAMCRT-04 | 空远程主机 | 无 | remoteHost="" | 调用 | 返回 `RteInvalidParameter` 或 `RteConnectFalse` | P3 |

---

### 8. RawRtdbRecvDatagramWarp — 接收数据流

**函数签名**：`func RawRtdbRecvDatagramWarp(handle DatagramHandle, cacheLen int32, remoteAddr string, timeout int32) ([]byte, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-DGRAMRCV-01 | 正常接收数据 | 数据流已创建且有对端发送 | cacheLen=1024, remoteAddr="127.0.0.1", timeout=5 | 调用 `RawRtdbRecvDatagramWarp` | 返回 `RteOk`，[]byte 含接收数据 | P1 |
| TC-DGRAMRCV-02 | 超时接收 | 数据流已创建，暂无数据 | timeout=1 | 调用 | 返回超时错误或空数据 | P2 |
| TC-DGRAMRCV-03 | cacheLen=0 | 数据流已创建 | cacheLen=0 | 调用 | 返回空数据或参数错误 | P3 |
| TC-DGRAMRCV-04 | 无效句柄 | handle=nil DatagramHandle | — | 调用 | 返回 `RteInvalidHandle` | P2 |
| TC-DGRAMRCV-05 | 空 remoteAddr | 数据流已创建 | remoteAddr="" | 调用 | 返回地址无效错误 | P3 |

---

### 9. RawRtdbRemoveDatagramHandleWarp — 删除数据流

**函数签名**：`func RawRtdbRemoveDatagramHandleWarp(handle DatagramHandle) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-DGRAMRMV-01 | 正常删除数据流 | 数据流已创建 | handle=创建返回的 DatagramHandle | 调用 `RawRtdbRemoveDatagramHandleWarp` | 返回 `RteOk` | P1 |
| TC-DGRAMRMV-02 | 删除后接收 | 已删除 | handle 同上 | 调用 `RawRtdbRecvDatagramWarp` | 返回数据流不存在错误 | P2 |
| TC-DGRAMRMV-03 | 重复删除 | 已删除 | handle 同上 | 再次调用 `RawRtdbRemoveDatagramHandleWarp` | 返回 `RteOk` 或错误 | P3 |
| TC-DGRAMRMV-04 | 无效句柄 | — | handle=nil | 调用 | 返回 `RteInvalidHandle` | P2 |

---

## 附录：API 覆盖清单

| API 名称 | 用例编号范围 | 覆盖状态 |
|---------|------------|---------|
| RawRtdbbSubscribeTagsExWarp | TC-SUBTAG-01~04 | 已覆盖 |
| RawRtdbbCancelSubscribeTagsWarp | TC-CANTAG-01~03 | 已覆盖 |
| RawRtdbsSubscribeSnapshotsEx64Warp | TC-SUBSNAP-01~05 | 已覆盖 |
| RawRtdbsSubscribeDeltaSnapshots64Warp | TC-SUBDELTA-01~04 | 已覆盖 |
| RawRtdbsChangeSubscribeSnapshotsWarp | TC-CHGSUB-01~04 | 已覆盖 |
| RawRtdbsCancelSubscribeSnapshotsWarp | TC-CANSNAP-01~03 | 已覆盖 |
| RawRtdbCreateDatagramHandleWarp | TC-DGRAMCRT-01~04 | 已覆盖 |
| RawRtdbRecvDatagramWarp | TC-DGRAMRCV-01~05 | 已覆盖 |
| RawRtdbRemoveDatagramHandleWarp | TC-DGRAMRMV-01~04 | 已覆盖 |
