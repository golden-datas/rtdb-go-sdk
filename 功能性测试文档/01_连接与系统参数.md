# 01. 连接与系统参数

> **模块说明**：本模块覆盖 API 版本查询、连接建立与断开、登录管理、连接事件订阅、系统参数读写、连接信息查询、时间格式化与解析、错误码翻译、超时设置、连接状态判断及 IP 格式化等相关 Raw API。

---

## 1. RawRtdbGetApiVersionWarp — 返回 API 版本号

**函数签名**：`func RawRtdbGetApiVersionWarp() (ApiVersion, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-APIVER-01 | 正常获取版本号 | 无 | 无参数 | 调用 `RawRtdbGetApiVersionWarp` | 返回 `RteOk`，`ApiVersion.Major >= 0`，`Minor >= 0`，`Beta >= 0` | P1 |

---

## 2. RawRtdbSetOptionWarp — 配置 API 库的行为参数

**函数签名**：`func RawRtdbSetOptionWarp(optionType RtdbApiOption, value int32) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-OPTION-01 | 设置自动重连选项 | 已获取有效连接句柄 | `optionType = RTDB_O_AUTOCONN`, `value = 1` | 调用 `RawRtdbSetOptionWarp(RtdbApiOptionAutoConn, 1)` | 返回 `RteOk` | P1 |
| TC-OPTION-02 | 设置无效选项值 | 已获取有效连接句柄 | `optionType = 99999`（非法值）, `value = 0` | 调用 `RawRtdbSetOptionWarp(99999, 0)` | 返回 `RteInvalidParameter` 或相应错误码 | P2 |

---

## 3. RawRtdbConnectWarp — 建立同 RTDB 数据库的网络连接

**函数签名**：`func RawRtdbConnectWarp(hostname string, port int32) (ConnectHandle, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-CONN-01 | 正常连接数据库 | RTDB 服务端已启动，网络可达 | `hostname = "127.0.0.1"`, `port = 9000` | 调用 `RawRtdbConnectWarp("127.0.0.1", 9000)` | 返回 `RteOk`，`ConnectHandle > 0` | P1 |
| TC-CONN-02 | 连接不存在的主机 | 无 | `hostname = "192.0.2.1"`（TEST-NET-1 不可达地址）, `port = 9000` | 调用 `RawRtdbConnectWarp("192.0.2.1", 9000)` | 返回 `RteConnectFalse` 或 `RteConnectTimeOut` | P1 |
| TC-CONN-03 | 连接非法端口 | 无 | `hostname = "127.0.0.1"`, `port = -1` | 调用 `RawRtdbConnectWarp("127.0.0.1", -1)` | 返回 `RteInvalidParameter` 或连接失败错误 | P2 |
| TC-CONN-04 | 连接空主机名 | 无 | `hostname = ""`, `port = 9000` | 调用 `RawRtdbConnectWarp("", 9000)` | 返回非 `RteOk` 错误码 | P2 |

---

## 4. RawRtdbLoginWarp — 以有效帐户登录数据库

**函数签名**：`func RawRtdbLoginWarp(handle ConnectHandle, user string, password string) (PrivGroup, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-LOGIN-01 | 管理员正常登录 | 已通过 `RawRtdbConnectWarp` 获取有效连接句柄 | `user = "sa"`, `password = "正确密码"` | 调用 `RawRtdbLoginWarp(handle, "sa", "正确密码")` | 返回 `RteOk`，`PrivGroup` 包含管理员权限 | P1 |
| TC-LOGIN-02 | 错误密码登录 | 已获取有效连接句柄 | `user = "sa"`, `password = "wrong_pwd"` | 调用 `RawRtdbLoginWarp(handle, "sa", "wrong_pwd")` | 返回 `RteWrongPassword` 或 `RteWrongLogin4/3/2/1` | P1 |
| TC-LOGIN-03 | 登录不存在的用户 | 已获取有效连接句柄 | `user = "notexist"`, `password = "any"` | 调用 `RawRtdbLoginWarp(handle, "notexist", "any")` | 返回 `RteUserNotExist` | P1 |
| TC-LOGIN-04 | 登录已锁定用户 | 已获取有效连接句柄；存在被锁定的用户 `locked_user` | `user = "locked_user"`, `password = "正确密码"` | 调用 `RawRtdbLoginWarp(handle, "locked_user", "正确密码")` | 返回 `RteUserIsLocked` | P2 |
| TC-LOGIN-05 | 未连接直接登录 | 使用无效句柄（如 `-1`） | `handle = -1`, `user = "sa"`, `password = "pwd"` | 调用 `RawRtdbLoginWarp(-1, "sa", "pwd")` | 返回 `RteInvalidHandle` | P2 |

---

## 5. RawRtdbDisconnectWarp — 断开同 RTDB 数据平台的连接

**函数签名**：`func RawRtdbDisconnectWarp(handle ConnectHandle) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-DISC-01 | 正常断开连接 | 已连接并登录 | 有效 `ConnectHandle` | 调用 `RawRtdbDisconnectWarp(handle)` | 返回 `RteOk` | P1 |
| TC-DISC-02 | 断开已断开的句柄 | 已调用过 Disconnect | 同一已断开句柄 | 再次调用 `RawRtdbDisconnectWarp(handle)` | 返回 `RteConnectFalse` 或 `RteInvalidHandle` | P2 |
| TC-DISC-03 | 断开无效句柄 | 无 | `handle = -1` | 调用 `RawRtdbDisconnectWarp(-1)` | 返回 `RteInvalidHandle` | P2 |

---

## 6. RawRtdbSubscribeConnectExWarp — 创建 API 调用连接事件订阅

**函数签名**：`func RawRtdbSubscribeConnectExWarp(handle ConnectHandle, options RtdbSubscribeOption, name string) (unsafe.Pointer, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-SUBCONN-01 | 正常创建连接事件订阅 | 已连接登录 | `options = RTDB_O_AUTOCONN`, `name = "test_sub"` | 调用 `RawRtdbSubscribeConnectExWarp(handle, RTDB_O_AUTOCONN, "test_sub")` | 返回 `RteOk`，返回非空 `unsafe.Pointer` | P1 |
| TC-SUBCONN-02 | 重复创建订阅 | 已创建订阅 | 同一句柄再次订阅 | 再次调用 `RawRtdbSubscribeConnectExWarp(handle, RTDB_O_AUTOCONN, "test_sub2")` | 返回 `RteHandleSubscribed` | P2 |

---

## 7. RawRtdbCancelSubscribeConnectWarp — 取消 API 调用连接事件订阅

**函数签名**：`func RawRtdbCancelSubscribeConnectWarp(handle ConnectHandle, param unsafe.Pointer) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-SUBCONN-03 | 正常取消订阅 | 已创建订阅 | 有效句柄及订阅时返回的 `param` | 调用 `RawRtdbCancelSubscribeConnectWarp(handle, param)` | 返回 `RteOk` | P1 |
| TC-SUBCONN-04 | 网络断开触发订阅回调 | 已创建订阅，使用独立句柄 | — | 强制关闭网络或停止服务端，等待回调触发 | 回调函数被调用，`event_type = RTDB_E_DISCONNECT` | P1 |

---

## 8. RawRtdbGetDbInfo1Warp — 获取字符串型数据库系统参数

**函数签名**：`func RawRtdbGetDbInfo1Warp(handle ConnectHandle, param RtdbParam) (ParamString, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-DBINFO-01 | 获取服务器描述（字符串参数） | 已连接登录 | `param = RTDB_PARAM_SERVER_DESCRIPTION` | 调用 `RawRtdbGetDbInfo1Warp(handle, param)` | 返回 `RteOk`，结果字符串非空 | P1 |
| TC-DBINFO-03 | 获取非法参数索引 | 已连接登录 | `param = 99999` | 调用 `RawRtdbGetDbInfo1Warp(handle, 99999)` | 返回 `RteInvalidParamIndex` | P2 |
| TC-DBINFO-04 | 未登录获取参数 | 仅 Connect 未 Login | 有效 `param` | 调用 `RawRtdbGetDbInfo1Warp(handle, param)` | 返回 `RteHaveNotLogin` 或 `RteAccessIsDenied` | P2 |

---

## 9. RawRtdbGetDbInfo2Warp — 获取整型数据库系统参数

**函数签名**：`func RawRtdbGetDbInfo2Warp(handle ConnectHandle, param RtdbParam) (ParamInt, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-DBINFO-02 | 获取最大连接数（整型参数） | 已连接登录 | `param = RTDB_PARAM_MAX_CONNECTIONS` | 调用 `RawRtdbGetDbInfo2Warp(handle, param)` | 返回 `RteOk`，`ParamInt > 0` | P1 |

---

## 10. RawRtdbSetDbInfo1Warp — 设置字符串型数据库系统参数

**函数签名**：`func RawRtdbSetDbInfo1Warp(handle ConnectHandle, param RtdbParam, value ParamString) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-SETDBINFO-01 | 管理员设置系统参数 | 管理员身份登录 | `param = RTDB_PARAM_SERVER_DESCRIPTION`, `value = "TestServer"` | 调用 `RawRtdbSetDbInfo1Warp(handle, param, "TestServer")` | 返回 `RteOk` | P1 |
| TC-SETDBINFO-02 | 普通用户设置参数（权限不足） | 普通用户登录 | 可写参数及值 | 调用 `RawRtdbSetDbInfo1Warp(handle, param, value)` | 返回 `RteAccessIsDenied` | P1 |
| TC-SETDBINFO-03 | 设置非法参数索引 | 管理员登录 | `param = 99999` | 调用 `RawRtdbSetDbInfo1Warp(handle, 99999, "")` | 返回 `RteInvalidSysParam` | P2 |

---

## 11. RawRtdbSetDbInfo2Warp — 设置整型数据库系统参数

**函数签名**：`func RawRtdbSetDbInfo2Warp(handle ConnectHandle, param RtdbParam, value ParamInt) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-SETDBINFO2-01 | 管理员设置整型参数 | 管理员身份登录 | `param = RTDB_PARAM_MAX_CONNECTIONS`, `value = 100` | 调用 `RawRtdbSetDbInfo2Warp(handle, param, 100)` | 返回 `RteOk` | P1 |
| TC-SETDBINFO2-02 | 普通用户设置整型参数（权限不足） | 普通用户登录 | 可写参数及值 | 调用 `RawRtdbSetDbInfo2Warp(handle, param, value)` | 返回 `RteAccessIsDenied` | P1 |
| TC-SETDBINFO2-03 | 设置非法整型参数索引 | 管理员登录 | `param = 99999` | 调用 `RawRtdbSetDbInfo2Warp(handle, 99999, 0)` | 返回 `RteInvalidSysParam` | P2 |

---

## 12. RawRtdbConnectionCountWarp — 获取 RTDB 服务器当前连接个数

**函数签名**：`func RawRtdbConnectionCountWarp(handle ConnectHandle, nodeNumber int32) (int32, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-CONNCOUNT-01 | 获取单机模式连接数 | 已连接登录；单机模式 | `nodeNumber = 0` | 调用 `RawRtdbConnectionCountWarp(handle, 0)` | 返回 `RteOk`，`count >= 1`（至少包含当前连接） | P1 |
| TC-CONNCOUNT-02 | 获取双活节点1连接数 | 已连接登录；双活模式 | `nodeNumber = 1` | 调用 `RawRtdbConnectionCountWarp(handle, 1)` | 返回 `RteOk`，`count >= 0` | P2 |

---

## 13. RawRtdbGetConnectionsWarp — 列出所有 socket 连接

**函数签名**：`func RawRtdbGetConnectionsWarp(handle ConnectHandle, nodeNumber int32, count int32) ([]SocketHandle, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-GETCONNS-01 | 获取所有连接句柄 | 已连接登录；存在多个客户端连接 | `nodeNumber = 0`, `count = 100` | 先调用 `RawRtdbConnectionCountWarp` 获取总数，再调用 `RawRtdbGetConnectionsWarp(handle, 0, count)` | 返回 `RteOk`，`[]SocketHandle` 长度与连接数一致，所有句柄值大于 0 | P1 |
| TC-GETCONNS-03 | count 为 0 时返回空数组 | 已连接登录 | `count = 0` | 调用 `RawRtdbGetConnectionsWarp(handle, 0, 0)` | 返回 `RteOk`，返回空数组 `[]SocketHandle{}` | P2 |

---

## 14. RawRtdbGetOwnConnectionWarp — 获取当前 socket 句柄

**函数签名**：`func RawRtdbGetOwnConnectionWarp(handle ConnectHandle, nodeNumber int32) (SocketHandle, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-GETCONNS-02 | 获取当前连接句柄 | 已连接登录 | `nodeNumber = 0` | 调用 `RawRtdbGetOwnConnectionWarp(handle, 0)` | 返回 `RteOk`，`SocketHandle > 0` | P1 |

---

## 15. RawRtdbGetConnectionInfoWarp — 获取指定连接的详细信息（IPv4）

**函数签名**：`func RawRtdbGetConnectionInfoWarp(handle ConnectHandle, nodeNumber int32, socket SocketHandle) (RtdbHostConnectInfo, error)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-CONNINFO-01 | 获取当前连接 IPv4 信息 | 已连接登录 | 当前句柄，`nodeNumber = 0`，自身 `socket` | 获取自身 `socket`，调用 `RawRtdbGetConnectionInfoWarp(handle, 0, socket)` | 返回 `RteOk`，`RtdbHostConnectInfo` 中 `Ip` 非空，`Port > 0` | P1 |
| TC-CONNINFO-02 | 获取无效 socket 信息 | 已连接登录 | `socket = -1` | 调用 `RawRtdbGetConnectionInfoWarp(handle, 0, -1)` | 返回 `RteInvalidSocketHandle` | P2 |

---

## 16. RawRtdbGetConnectionInfoIpv6Warp — 获取指定连接的详细信息（IPv6）

**函数签名**：`func RawRtdbGetConnectionInfoIpv6Warp(handle ConnectHandle, nodeNumber int32, socket SocketHandle) (RtdbHostConnectInfoIpv6, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-CONNINFO-03 | 获取 IPv6 连接信息 | 已连接登录；服务器支持 IPv6 | 有效 `socket` | 调用 `RawRtdbGetConnectionInfoIpv6Warp(handle, 0, socket)` | 返回 `RteOk`，`RtdbHostConnectInfoIpv6` 中 IPv6 地址字段非空 | P2 |

---

## 17. RawRtdbOsType — 获取服务器操作系统类型

**函数签名**：`func RawRtdbOsType(handle ConnectHandle) (RtdbOsType, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-OSTYPE-01 | 获取服务器 OS 类型 | 已连接登录 | 有效句柄 | 调用 `RawRtdbOsType(handle)` | 返回 `RteOk`，`RtdbOsType` 为 `RTDB_OS_WINDOWS`、`RTDB_OS_LINUX` 或 `RTDB_OS_INVALID` 之一 | P1 |

---

## 18. RawRtdbGetHandleInfoWarp — 获取连接句柄所连接的服务器相关信息

**函数签名**：`func RawRtdbGetHandleInfoWarp(handle ConnectHandle) (RtdbHandleInfo, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-HANDLEINFO-01 | 获取句柄信息 | 已连接登录 | 有效句柄 | 调用 `RawRtdbGetHandleInfoWarp(handle)` | 返回 `RteOk`，`RtdbHandleInfo` 中服务端版本号、主机名等关键字段非空 | P1 |

---

## 19. RawRtdbHostTime64Warp — 获取 RTDB 服务器当前 UTC 时间（64位时间戳）

**函数签名**：`func RawRtdbHostTime64Warp(handle ConnectHandle) (TimestampType, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-HOSTTIME-01 | 获取服务器当前时间 | 已连接登录 | 有效句柄 | 调用 `RawRtdbHostTime64Warp(handle)`，与本地时间做对比（允许网络延迟误差） | 返回 `RteOk`，`TimestampType > 0`，与本地 UTC 时间差值在合理范围内（如 < 60 秒） | P1 |
| TC-HOSTTIME-02 | 未登录获取时间 | 仅 Connect 未 Login | 有效连接句柄 | 调用 `RawRtdbHostTime64Warp(handle)` | 返回 `RteHaveNotLogin` 或 `RteAccessIsDenied` | P2 |

---

## 20. RawRtdbFormatTimespanWarp — 时间跨度值格式化为字符串

**函数签名**：`func RawRtdbFormatTimespanWarp(timespan int32) (string, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-TIMESPAN-01 | 格式化 10 秒 | 无 | `timespan = 10` | 调用 `RawRtdbFormatTimespanWarp(10)` | 返回 `RteOk`，结果字符串为 `"10s"` | P1 |
| TC-TIMESPAN-02 | 格式化 60 秒 | 无 | `timespan = 60` | 调用 `RawRtdbFormatTimespanWarp(60)` | 返回 `RteOk`，结果字符串为 `"1n"`（1分钟） | P1 |

---

## 21. RawRtdbParseTimespanWarp — 解析时间跨度字符串

**函数签名**：`func RawRtdbParseTimespanWarp(tStr string) (DateTimeType, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-TIMESPAN-03 | 解析 2 分钟 | 无 | `tStr = "2n"` | 调用 `RawRtdbParseTimespanWarp("2n")` | 返回 `RteOk`，`DateTimeType = 120` | P1 |
| TC-TIMESPAN-04 | 解析无效时间字符串 | 无 | `tStr = "invalid"` | 调用 `RawRtdbParseTimespanWarp("invalid")` | 返回 `RteInvalidTimeFormat` | P2 |

---

## 22. RawRtdbParseTimeWarp — 根据时间格式字符串解析时间值

**函数签名**：`func RawRtdbParseTimeWarp(tStr string) (TimestampType, SubtimeType, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-PARSETIME-01 | 解析绝对时间 | 无 | `tStr = "2024-01-01 08:00:00"` | 调用 `RawRtdbParseTimeWarp("2024-01-01 08:00:00")` | 返回 `RteOk`，`TimestampType` 对应 2024-01-01 08:00:00 的 Unix 时间戳，`SubtimeType = 0` | P1 |
| TC-PARSETIME-02 | 解析相对时间 "*-1d" | 无 | `tStr = "*-1d"` | 调用 `RawRtdbParseTimeWarp("*-1d")` | 返回 `RteOk`，`TimestampType` 约等于当前时间减 86400 秒 | P1 |
| TC-PARSETIME-03 | 解析无效时间字符串 | 无 | `tStr = "not_a_time"` | 调用 `RawRtdbParseTimeWarp("not_a_time")` | 返回 `RteInvalidTimeFormat` | P2 |

---

## 23. RawRtdbFormatMessageWarp — 获取错误码的简短描述

**函数签名**：`func RawRtdbFormatMessageWarp(err RtdbError) (string, string)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-FMTMSG-01 | 格式化成功错误码 | 无 | `err = RteOk` | 调用 `RawRtdbFormatMessageWarp(RteOk)` | 函数名非空，消息描述包含"成功"或"Ok"相关字样 | P1 |
| TC-FMTMSG-02 | 格式化已知错误码 | 无 | `err = RtePointNotFound` | 调用 `RawRtdbFormatMessageWarp(RtePointNotFound)` | 消息描述包含"标签点不存在"或"PointNotFound"相关字样 | P1 |

---

## 24. RawRtdbJobMessageWarp — 获取任务的简短描述

**函数签名**：`func RawRtdbJobMessageWarp(jobID int32) (string, string)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-JOBMSG-01 | 获取任务描述 | 无 | `jobID = 0` | 调用 `RawRtdbJobMessageWarp(0)` | 函数名与描述字符串非空（即使为未知任务也应返回默认值） | P1 |

---

## 25. RawRtdbSetTimeoutWarp — 设置连接超时时间

**函数签名**：`func RawRtdbSetTimeoutWarp(handle ConnectHandle, socket SocketHandle, timeout DateTimeType) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-TIMEOUT-01 | 设置并读取超时时间 | 已连接登录 | `socket = 自身socket`, `timeout = 30` | 先调用 `RawRtdbSetTimeoutWarp(handle, socket, 30)`，再调用 `RawRtdbGetTimeoutWarp(handle, socket)` | 步骤1返回 `RteOk`，步骤2返回 `RteOk`，读取到的 `DateTimeType = 30` | P1 |
| TC-TIMEOUT-02 | 设置超时时间为 0（永不超时） | 已连接登录 | `timeout = 0` | 调用 `RawRtdbSetTimeoutWarp(handle, socket, 0)` | 返回 `RteOk` | P2 |

---

## 26. RawRtdbGetTimeoutWarp — 获取连接超时时间

**函数签名**：`func RawRtdbGetTimeoutWarp(handle ConnectHandle, socket SocketHandle) (DateTimeType, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-TIMEOUT-03 | 读取默认超时时间 | 已连接登录 | `socket = 自身socket` | 调用 `RawRtdbGetTimeoutWarp(handle, socket)` | 返回 `RteOk`，`DateTimeType > 0` | P1 |
| TC-TIMEOUT-04 | 读取无效 socket 超时 | 已连接登录 | `socket = -1` | 调用 `RawRtdbGetTimeoutWarp(handle, -1)` | 返回 `RteInvalidSocketHandle` 或相应错误 | P2 |

---

## 27. RawRtdbKillConnectionWarp — 断开已知 socket 连接

**函数签名**：`func RawRtdbKillConnectionWarp(handle ConnectHandle, socket SocketHandle) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-KILLCONN-01 | 断开其他客户端连接 | 已连接登录（管理员权限）；存在其他客户端连接 | 其他客户端的 `socket` | 获取其他客户端 socket，调用 `RawRtdbKillConnectionWarp(handle, otherSocket)` | 返回 `RteOk` | P1 |
| TC-KILLCONN-02 | 断开自身连接 | 已连接登录 | 自身 `socket` | 调用 `RawRtdbKillConnectionWarp(handle, ownSocket)` | 返回 `RteKillConnectionFailed` | P2 |
| TC-KILLCONN-03 | 普通用户断开连接（权限不足） | 普通用户登录 | 其他客户端 `socket` | 调用 `RawRtdbKillConnectionWarp(handle, otherSocket)` | 返回 `RteAccessIsDenied` | P1 |

---

## 28. RawRtdbJudgeConnectStatusWarp — 判断连接是否可用

**函数签名**：`func RawRtdbJudgeConnectStatusWarp(handle ConnectHandle) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-JUDGE-01 | 判断正常连接状态 | 已连接登录 | 有效句柄 | 调用 `RawRtdbJudgeConnectStatusWarp(handle)` | 返回 `RteOk` | P1 |
| TC-JUDGE-02 | 判断已断开连接 | 已调用 Disconnect | 同一已断开句柄 | 调用 `RawRtdbJudgeConnectStatusWarp(handle)` | 返回 `RteConnectFalse` 或 `RteInvalidHandle` | P2 |

---

## 29. RawRtdbFormatIpaddrWarp — 将整型 IP 转换为字符串形式的 IP

**函数签名**：`func RawRtdbFormatIpaddrWarp(ip uint32) string`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-FMTIP-01 | 转换回环地址 | 无 | `ip = 0x7F000001` (127.0.0.1) | 调用 `RawRtdbFormatIpaddrWarp(0x7F000001)` | 返回字符串 `"127.0.0.1"` | P1 |
| TC-FMTIP-02 | 转换零地址 | 无 | `ip = 0` | 调用 `RawRtdbFormatIpaddrWarp(0)` | 返回字符串 `"0.0.0.0"` | P1 |
| TC-FMTIP-03 | 转换广播地址 | 无 | `ip = 0xFFFFFFFF` | 调用 `RawRtdbFormatIpaddrWarp(0xFFFFFFFF)` | 返回字符串 `"255.255.255.255"` | P1 |

---

## 附录：API 覆盖清单

| API 名称 | 用例编号范围 | 覆盖状态 |
|---------|------------|---------|
| RawRtdbGetApiVersionWarp | TC-APIVER-01 | 已覆盖 |
| RawRtdbSetOptionWarp | TC-OPTION-01~02 | 已覆盖 |
| RawRtdbConnectWarp | TC-CONN-01~04 | 已覆盖 |
| RawRtdbLoginWarp | TC-LOGIN-01~05 | 已覆盖 |
| RawRtdbDisconnectWarp | TC-DISC-01~03 | 已覆盖 |
| RawRtdbSubscribeConnectExWarp | TC-SUBCONN-01~02 | 已覆盖 |
| RawRtdbCancelSubscribeConnectWarp | TC-SUBCONN-03~04 | 已覆盖 |
| RawRtdbGetDbInfo1Warp | TC-DBINFO-01,03~04 | 已覆盖 |
| RawRtdbGetDbInfo2Warp | TC-DBINFO-02 | 已覆盖 |
| RawRtdbSetDbInfo1Warp | TC-SETDBINFO-01~03 | 已覆盖 |
| RawRtdbSetDbInfo2Warp | TC-SETDBINFO2-01~03 | 已覆盖 |
| RawRtdbConnectionCountWarp | TC-CONNCOUNT-01~02 | 已覆盖 |
| RawRtdbGetConnectionsWarp | TC-GETCONNS-01,03 | 已覆盖 |
| RawRtdbGetOwnConnectionWarp | TC-GETCONNS-02 | 已覆盖 |
| RawRtdbGetConnectionInfoWarp | TC-CONNINFO-01~02 | 已覆盖 |
| RawRtdbGetConnectionInfoIpv6Warp | TC-CONNINFO-03 | 已覆盖 |
| RawRtdbOsType | TC-OSTYPE-01 | 已覆盖 |
| RawRtdbGetHandleInfoWarp | TC-HANDLEINFO-01 | 已覆盖 |
| RawRtdbHostTime64Warp | TC-HOSTTIME-01~02 | 已覆盖 |
| RawRtdbFormatTimespanWarp | TC-TIMESPAN-01~02 | 已覆盖 |
| RawRtdbParseTimespanWarp | TC-TIMESPAN-03~04 | 已覆盖 |
| RawRtdbParseTimeWarp | TC-PARSETIME-01~03 | 已覆盖 |
| RawRtdbFormatMessageWarp | TC-FMTMSG-01~02 | 已覆盖 |
| RawRtdbJobMessageWarp | TC-JOBMSG-01 | 已覆盖 |
| RawRtdbSetTimeoutWarp | TC-TIMEOUT-01~02 | 已覆盖 |
| RawRtdbGetTimeoutWarp | TC-TIMEOUT-03~04 | 已覆盖 |
| RawRtdbKillConnectionWarp | TC-KILLCONN-01~03 | 已覆盖 |
| RawRtdbJudgeConnectStatusWarp | TC-JUDGE-01~02 | 已覆盖 |
| RawRtdbFormatIpaddrWarp | TC-FMTIP-01~03 | 已覆盖 |
