# 09. 存档管理

> **模块说明**：本模块覆盖历史存档文件的创建、追加、移除、查询、状态监控、维护整理、备份移动、索引转换，以及后台大任务（BigJob）的查询与取消等相关 Raw API。

---

## 一、存档基础操作

### 1. RawRtdbaGetArchivesCountWarp — 获取存档文件数量

**函数签名**：`func RawRtdbaGetArchivesCountWarp(handle ConnectHandle) (int32, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-ARCCNT-01 | 正常获取存档数量 | 已连接登录，服务端存在若干存档 | — | 调用 `RawRtdbaGetArchivesCountWarp` | 返回 `RteOk`，count >= 0 | P1 |
| TC-ARCCNT-02 | 无效句柄 | handle = -1 | — | 调用 | 返回 `RteInvalidHandle` | P2 |
| TC-ARCCNT-03 | 未登录 | 仅连接未登录 | — | 调用 | 返回 `RteHaveNotLogin` | P2 |
| TC-ARCCNT-04 | 无权限 | 使用无管理权限的普通用户登录 | — | 调用 | 返回 `RteAccessIsDenied` | P2 |

---

### 2. RawRtdbaCreateRangedArchive64Warp — 新建指定时间范围的历史存档文件

**函数签名**：`func RawRtdbaCreateRangedArchive64Warp(handle ConnectHandle, path string, file string, begin TimestampType, end TimestampType, mbSize int32) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-ARCCRT-01 | 正常创建时间范围存档 | 已登录，目录存在 | path="/data/", file="test2024.rdf", begin=1700000000, end=1700003600, mbSize=100 | 调用 | 返回 `RteOk`，存档文件在服务端生成 | P1 |
| TC-ARCCRT-02 | 路径不以分隔符结尾 | 已登录 | path="/data", file="test.rdf" | 调用 | 返回路径格式错误或自动修正后成功 | P2 |
| TC-ARCCRT-03 | 起始时间晚于终止时间 | 已登录 | begin=1700003600, end=1700000000 | 调用 | 返回时间范围错误 | P2 |
| TC-ARCCRT-04 | 零大小文件 | 已登录 | mbSize=0 | 调用 | 返回参数错误或创建失败 | P3 |
| TC-ARCCRT-05 | 目录不存在 | 已登录 | path="/nonexist/" | 调用 | 返回路径不存在错误 | P2 |
| TC-ARCCRT-06 | 文件名重复 | 已登录，该文件已存在 | file="test2024.rdf" | 调用 | 返回文件已存在错误 | P2 |

---

### 3. RawRtdbaAppendArchiveWarp — 追加磁盘上的历史存档文件到历史数据库

**函数签名**：`func RawRtdbaAppendArchiveWarp(handle ConnectHandle, path string, file string, state RtdbArchiveState) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-ARCAPP-01 | 正常追加普通存档 | 已登录，磁盘存在合法.rdf文件 | path="/data/", file="append.rdf", state=RTDB_NORMAL_ARCHIVE | 调用 | 返回 `RteOk`，该存档进入队列 | P1 |
| TC-ARCAPP-02 | 追加为只读存档 | 已登录 | state=RTDB_READONLY_ARCHIVE | 调用 | 返回 `RteOk`，状态为只读 | P2 |
| TC-ARCAPP-03 | 文件不存在 | 已登录 | file="notexist.rdf" | 调用 | 返回文件不存在错误 | P2 |
| TC-ARCAPP-04 | 追加与已有存档时间交叠的文件 | 已登录，该时间范围已被其他存档覆盖 | — | 调用 | 返回时间交叠错误 | P2 |
| TC-ARCAPP-05 | 无效状态值 | 已登录 | state=999 | 调用 | 返回无效参数错误 | P3 |

---

### 4. RawRtdbaRemoveArchiveWarp — 从历史数据库中移出历史存档文件

**函数签名**：`func RawRtdbaRemoveArchiveWarp(handle ConnectHandle, path string, file string) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-ARCRMV-01 | 正常移出存档 | 已登录，该存档已在队列中 | path="/data/", file="remove.rdf" | 调用 | 返回 `RteOk`，队列中不再包含该文件 | P1 |
| TC-ARCRMV-02 | 移出活动文件 | 已登录，该文件为当前活动文件 | — | 调用 | 返回活动文件不允许移出错误，或自动切换后移出 | P2 |
| TC-ARCRMV-03 | 移出不存在的存档 | 已登录 | file="notexist.rdf" | 调用 | 返回存档不存在错误 | P2 |
| TC-ARCRMV-04 | 重复移出 | 已移出 | 同上 | 再次调用 | 返回存档不存在错误 | P3 |

---

### 5. RawRtdbaShiftActivedWarp — 切换活动文件

**函数签名**：`func RawRtdbaShiftActivedWarp(handle ConnectHandle) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-ARCSFT-01 | 正常切换活动文件 | 已登录，当前活动文件即将写满 | — | 调用 | 返回 `RteOk`，原活动文件状态变为 NORMAL，新的活动文件产生 | P1 |
| TC-ARCSFT-02 | 无可用未使用文件时切换 | 已登录，右侧无未使用文件 | — | 调用 | 原活动文件右侧 NORMAL 文件被激活 | P2 |
| TC-ARCSFT-03 | 仅有一个活动文件 | 已登录，存档队列中只有一个文件 | — | 调用 | 返回错误或保持现状 | P3 |
| TC-ARCSFT-04 | 无效句柄 | handle = -1 | — | 调用 | 返回 `RteInvalidHandle` | P2 |

---

## 二、存档查询与信息获取

### 6. RawRtdbaGetArchivesWarp — 获取存档文件的路径、名称、状态和最早允许写入时间

**函数签名**：`func RawRtdbaGetArchivesWarp(handle ConnectHandle, maxCount int32) ([]string, []string, []RtdbArchiveState, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-ARCLST-01 | 正常获取全部存档 | 已登录，存在多个存档 | maxCount=100 | 调用 | 返回 `RteOk`，三个切片长度一致，状态包含 ACTIVE/NORMAL/READONLY | P1 |
| TC-ARCLST-02 | maxCount 为 0 | 已登录 | maxCount=0 | 调用 | 返回空切片和 `RteOk` | P2 |
| TC-ARCLST-03 | maxCount 小于实际数量 | 已登录，实际存档 10 个 | maxCount=5 | 调用 | 返回前 5 个存档信息 | P2 |
| TC-ARCLST-04 | 无存档 | 已登录，服务端无任何存档 | maxCount=100 | 调用 | 返回空切片和 `RteOk` | P2 |

---

### 7. RawRtdbaGetArchivesInfoWarp — 获取存档信息

**函数签名**：`func RawRtdbaGetArchivesInfoWarp(handle ConnectHandle, count int32) ([]string, []string, []RtdbHeaderPage, []RtdbError, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-ARCINF-01 | 正常获取存档详细信息 | 已登录，已知存在 3 个存档 | count=3 | 调用 | 返回 `RteOk`，HeaderPage 包含有效版本、容量信息 | P1 |
| TC-ARCINF-02 | count 大于实际数量 | 已登录，实际 2 个 | count=5 | 调用 | 返回实际数量的信息，多余项 errs 非 0 | P2 |
| TC-ARCINF-03 | count 为 0 | 已登录 | count=0 | 调用 | 返回空切片和 `RteOk` | P3 |

---

### 8. RawRtdbaGetArchivesPerfDataWarp — 获取存档的实时性能监控数据

**函数签名**：`func RawRtdbaGetArchivesPerfDataWarp(handle ConnectHandle, count int32) ([]string, []string, []RtdbArchivePerfData, []RtdbArchivePerfData, []RtdbError, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-ARCPRF-01 | 正常获取性能数据 | 已登录，存在存档且近期有读写 | count=实际数量 | 调用 | 返回 `RteOk`，real_time_datas 与 total_datas 结构完整 | P1 |
| TC-ARCPRF-02 | 获取无读写存档的性能 | 已登录，某存档长期无访问 | count 包含该存档 | 调用 | 对应 perf data 读写计数为 0 或保持不变 | P2 |
| TC-ARCPRF-03 | count 为 0 | 已登录 | count=0 | 调用 | 返回空切片和 `RteOk` | P3 |

---

### 9. RawRtdbaGetArchivesStatusWarp — 获取存档状态

**函数签名**：`func RawRtdbaGetArchivesStatusWarp(handle ConnectHandle) (RtdbArchiveState, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-ARCSTS-01 | 正常获取整体状态 | 已登录 | — | 调用 | 返回 `RteOk`，状态为有效枚举值 | P1 |
| TC-ARCSTS-02 | 无存档时获取状态 | 已登录，服务端无历史存档 | — | 调用 | 返回 `RteOk`，状态可能为 INVALID 或特定空状态 | P2 |
| TC-ARCSTS-03 | 无效句柄 | handle=-1 | — | 调用 | 返回 `RteInvalidHandle` | P2 |

---

### 10. RawRtdbaGetArchiveInfoWarp — 获取存档文件及其附属文件的详细信息

**函数签名**：`func RawRtdbaGetArchiveInfoWarp(handle ConnectHandle, path string, file string, fileId int32) (*RtdbHeaderPage, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-ARCGI-01 | 获取主文件信息 | 已登录，存档存在 | fileId=0 | 调用 | 返回 `RteOk`，HeaderPage 非 nil | P1 |
| TC-ARCGI-02 | 获取附属文件信息 | 已登录，该存档存在附属文件 | fileId=1 | 调用 | 返回 `RteOk`，HeaderPage 非 nil | P2 |
| TC-ARCGI-03 | 获取不存在的 fileId | 已登录 | fileId=99 | 调用 | 返回无效参数或不存在错误 | P3 |
| TC-ARCGI-04 | 存档文件不存在 | 已登录 | file="noexist.rdf" | 调用 | 返回文件不存在错误 | P2 |

---

## 三、存档维护与配置

### 11. RawRtdbaUpdateArchiveWarp — 修改存档文件的可配置项

**函数签名**：`func RawRtdbaUpdateArchiveWarp(handle ConnectHandle, path string, file string, ratedCapacity int32, exCapacity int32, autoMerge int16, autoArrange int16) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-ARCUPD-01 | 修改额定容量和附属文件大小 | 已登录 | ratedCapacity=200, exCapacity=50, autoMerge=1, autoArrange=1 | 调用 | 返回 `RteOk`，后续查询配置已更新 | P1 |
| TC-ARCUPD-02 | 传入 0 表示不修改 | 已登录 | ratedCapacity=0, exCapacity=0 | 调用 | 返回 `RteOk`，原配置保持不变 | P2 |
| TC-ARCUPD-03 | 修改不存在的存档 | 已登录 | file="noexist.rdf" | 调用 | 返回存档不存在错误 | P2 |
| TC-ARCUPD-04 | 无效句柄 | handle=-1 | — | 调用 | 返回 `RteInvalidHandle` | P2 |

---

### 12. RawRtdbaArrangeArchiveWarp — 整理存档文件

**函数签名**：`func RawRtdbaArrangeArchiveWarp(handle ConnectHandle, path string, file string) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-ARCARR-01 | 正常整理存档 | 已登录，存档存在且数据块分散 | path="/data/", file="arrange.rdf" | 调用 | 返回 `RteOk`，整理后台任务启动或同步完成 | P1 |
| TC-ARCARR-02 | 整理已高度紧凑的存档 | 已登录，刚刚整理过 | 同上 | 调用 | 返回 `RteOk`，任务快速完成 | P2 |
| TC-ARCARR-03 | 整理不存在的存档 | 已登录 | file="noexist.rdf" | 调用 | 返回存档不存在错误 | P2 |
| TC-ARCARR-04 | 无权限 | 已登录，普通用户 | — | 调用 | 返回 `RteAccessIsDenied` | P2 |

---

### 13. RawRtdbaReindexArchiveWarp — 重建存档文件索引

**函数签名**：`func RawRtdbaReindexArchiveWarp(handle ConnectHandle, path string, file string) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-ARCIDX-01 | 正常重建索引 | 已登录，存档存在 | path="/data/", file="reindex.rdf" | 调用 | 返回 `RteOk`，索引重建任务完成 | P1 |
| TC-ARCIDX-02 | 索引损坏后重建 | 已登录，模拟索引损坏场景 | — | 调用 | 返回 `RteOk`，数据可正常读取 | P1 |
| TC-ARCIDX-03 | 重建不存在的存档 | 已登录 | file="noexist.rdf" | 调用 | 返回存档不存在错误 | P2 |
| TC-ARCIDX-04 | 重建过程中查询 | 已登录，重建任务进行中 | — | 调用 `QueryBigJob` | 返回 RTDB_REINDEX 状态及进度 | P2 |

---

### 14. RawRtdbaBackupArchiveWarp — 备份主存档文件及其附属文件

**函数签名**：`func RawRtdbaBackupArchiveWarp(handle ConnectHandle, path string, file string, dest string) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-ARCBKP-01 | 正常备份到指定目录 | 已登录，dest 目录存在 | dest="/backup/" | 调用 | 返回 `RteOk`，目标目录出现同名文件及附属文件 | P1 |
| TC-ARCBKP-02 | 备份到不存在目录 | 已登录 | dest="/nonexist/" | 调用 | 返回目录不存在错误或自动创建 | P2 |
| TC-ARCBKP-03 | 目标空间不足 | 已登录，dest 磁盘空间不足 | — | 调用 | 返回磁盘空间不足错误 | P2 |
| TC-ARCBKP-04 | 备份不存在的存档 | 已登录 | file="noexist.rdf" | 调用 | 返回存档不存在错误 | P2 |

---

### 15. RawRtdbaMoveArchiveWarp — 将存档文件移动到指定目录

**函数签名**：`func RawRtdbaMoveArchiveWarp(handle ConnectHandle, path string, file string, dest string) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-ARCMOV-01 | 正常移动存档 | 已登录，dest 目录存在且空间充足 | dest="/newdata/" | 调用 | 返回 `RteOk`，原位置文件消失，目标位置出现 | P1 |
| TC-ARCMOV-02 | 移动到同一目录（改名） | 已登录 | path="/data/", dest="/data/" | 调用 | 返回目标与源相同错误或视为无操作 | P3 |
| TC-ARCMOV-03 | 目标目录不存在 | 已登录 | dest="/nonexist/" | 调用 | 返回目录不存在错误 | P2 |
| TC-ARCMOV-04 | 移动活动文件 | 已登录，file 为当前活动文件 | — | 调用 | 返回活动文件不可移动错误或先切换再移动 | P2 |

---

### 16. RawRtdbaConvertIndexWarp — 为存档文件转换索引格式

**函数签名**：`func RawRtdbaConvertIndexWarp(handle ConnectHandle, path string, file string) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-ARCCVT-01 | 正常转换旧版索引 | 已登录，存档为老版本索引格式 | — | 调用 | 返回 `RteOk`，查询性能提升 | P1 |
| TC-ARCCVT-02 | 重复转换（已是新版） | 已登录，存档已是新版索引 | — | 调用 | 返回 `RteOk` 或提示无需转换 | P2 |
| TC-ARCCVT-03 | 转换不存在的存档 | 已登录 | file="noexist.rdf" | 调用 | 返回存档不存在错误 | P2 |
| TC-ARCCVT-04 | 转换过程中查询进度 | 已登录，大存档转换中 | — | 调用 `QueryBigJob` | 返回 RTDB_CONVERT 或相关任务状态 | P2 |

---

## 四、后台大任务（BigJob）管理

### 17. RawRtdbaQueryBigJob64Warp — 查询进程正在执行的后台任务类型、状态和进度

**函数签名**：`func RawRtdbaQueryBigJob64Warp(handle ConnectHandle, processName RtdbProcess) (string, string, BigJobName, RtdbError, TimestampType, float32, RtdbError)`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-JOBQRY-01 | 查询无任务的进程 | 已登录，所选进程空闲 | processName=RTDB_SERVER | 调用 | 返回 `RteOk`，job 为无效/空任务标识 | P1 |
| TC-JOBQRY-02 | 查询正在整理的进程 | 已登录，正在执行 Arrange | processName=RTDB_SERVER | 调用 | 返回 `RteOk`，job=RTDB_ARRANGE，progress 在 0~100 之间 | P1 |
| TC-JOBQRY-03 | 查询正在重建索引的进程 | 已登录，正在执行 Reindex | processName=RTDB_SERVER | 调用 | 返回 `RteOk`，job=RTDB_REINDEX | P2 |
| TC-JOBQRY-04 | 查询备份任务 | 已登录，正在执行 Backup | processName=RTDB_SERVER | 调用 | 返回 `RteOk`，path 与 file 字段有效，job=RTDB_BACKUP | P2 |
| TC-JOBQRY-05 | 无效进程标识 | 已登录 | processName=99999 | 调用 | 返回无效参数错误 | P3 |
| TC-JOBQRY-06 | 任务完成后查询 | 已登录，备份刚完成 | processName=RTDB_SERVER | 调用 | 返回 `RteOk`，state 为完成状态，endTime 为实际完成时间 | P2 |

---

### 18. RawRtdbaCancelBigJobWarp — 取消进程正在执行的后台任务

**函数签名**：`func RawRtdbaCancelBigJobWarp(handle ConnectHandle, process RtdbProcess) RtdbError`

| 用例编号 | 用例标题 | 前置条件 | 输入数据 | 操作步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|---------|---------|--------|
| TC-JOBCAN-01 | 正常取消正在执行的任务 | 已登录，QueryBigJob 显示有进行中的 Arrange | process=RTDB_SERVER | 调用 | 返回 `RteOk`，再次查询任务已消失或状态为取消 | P1 |
| TC-JOBCAN-02 | 取消空闲进程 | 已登录，该进程无任务 | process=RTDB_SERVER | 调用 | 返回无任务可取消或 `RteOk` | P2 |
| TC-JOBCAN-03 | 取消后再次取消 | 已登录，已取消过 | process=RTDB_SERVER | 再次调用 | 返回无任务错误或 `RteOk` | P3 |
| TC-JOBCAN-04 | 无权限取消 | 已登录，普通用户 | — | 调用 | 返回 `RteAccessIsDenied` | P2 |

---

## 附录：API 覆盖清单

| API 名称 | 用例编号范围 | 覆盖状态 |
|---------|------------|---------|
| RawRtdbaGetArchivesCountWarp | TC-ARCCNT-01~04 | 已覆盖 |
| RawRtdbaCreateRangedArchive64Warp | TC-ARCCRT-01~06 | 已覆盖 |
| RawRtdbaAppendArchiveWarp | TC-ARCAPP-01~05 | 已覆盖 |
| RawRtdbaRemoveArchiveWarp | TC-ARCRMV-01~04 | 已覆盖 |
| RawRtdbaShiftActivedWarp | TC-ARCSFT-01~04 | 已覆盖 |
| RawRtdbaGetArchivesWarp | TC-ARCLST-01~04 | 已覆盖 |
| RawRtdbaGetArchivesInfoWarp | TC-ARCINF-01~03 | 已覆盖 |
| RawRtdbaGetArchivesPerfDataWarp | TC-ARCPRF-01~03 | 已覆盖 |
| RawRtdbaGetArchivesStatusWarp | TC-ARCSTS-01~03 | 已覆盖 |
| RawRtdbaGetArchiveInfoWarp | TC-ARCGI-01~04 | 已覆盖 |
| RawRtdbaUpdateArchiveWarp | TC-ARCUPD-01~04 | 已覆盖 |
| RawRtdbaArrangeArchiveWarp | TC-ARCARR-01~04 | 已覆盖 |
| RawRtdbaReindexArchiveWarp | TC-ARCIDX-01~04 | 已覆盖 |
| RawRtdbaBackupArchiveWarp | TC-ARCBKP-01~04 | 已覆盖 |
| RawRtdbaMoveArchiveWarp | TC-ARCMOV-01~04 | 已覆盖 |
| RawRtdbaConvertIndexWarp | TC-ARCCVT-01~04 | 已覆盖 |
| RawRtdbaQueryBigJob64Warp | TC-JOBQRY-01~06 | 已覆盖 |
| RawRtdbaCancelBigJobWarp | TC-JOBCAN-01~04 | 已覆盖 |
